name: Apply Battle Resume Patch
on:
  push:
    branches:
      - main
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo battle-resume-runner-ok
