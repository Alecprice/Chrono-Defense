import React,{useState}from'react';
const steps=[['₡','Credits','Credits buy defenses. Enemies and cleared waves replenish your budget.'],['⚡','Power Grid','Combat towers consume Power. Relays and Fusion Reactors increase total grid capacity.'],['🛡️','Shield + Core','Shield absorbs leaks and recharges between waves. Damage reaching Core Integrity is permanent for that map.'],['🥷','Stealth','Cloaked enemies cannot be targeted normally. Build Detector Arrays to reveal them.'],['💻','Hackers + EMP','Hackers delay individual towers. EMP carriers can temporarily disrupt the whole defense grid.'],['💠','Data Cores','Earn up to three Data Cores per map. Complete all 25 sectors to unlock Space.']];
export function FutureTutorial({onComplete}){const[index,setIndex]=useState(0),[icon,title,copy]=steps[index];return <div className="future-tutorial"><div><small>ERA III SYSTEM TRAINING • {index+1}/{steps.length}</small><span>{icon}</span><h2>{title}</h2><p>{copy}</p><div className="tutorial-dots">{steps.map((_,i)=><i key={i} className={i===index?'active':''}/>)}</div><footer><button onClick={onComplete}>Skip</button><button onClick={()=>index===steps.length-1?onComplete():setIndex(i=>i+1)}>{index===steps.length-1?'CONNECT':'Next →'}</button></footer></div></div>}
