import React,{useEffect,useMemo,useRef,useState}from'react';
import{futureTowers}from'../data/worlds/future/towers.js';import{futureMaps}from'../data/worlds/future/maps.js';import{buildFutureWave,summarizeFutureWave,FUTURE_WAVES}from'../data/worlds/future/waves.js';import{getFutureLayout,futureCellCenter}from'../data/worlds/future/layouts.js';import{distance}from'../core/combat.js';
const CELLS=Array.from({length:60},(_,i)=>i),START_CREDITS=650,BASE_POWER=35,MAX_SHIELD=250,MAX_CORE=200;
function pathLength(path){let t=0;for(let i=0;i<path.length-1;i++)t+=distance(futureCellCenter(path[i]),futureCellCenter(path[i+1]));return t}function point(path,travel){const pts=path.map(futureCellCenter),total=pathLength(path);let left=Math.max(0,travel);for(let i=0;i<pts.length-1;i++){const len=distance(pts[i],pts[i+1]);if(left<=len){const r=left/len;return{x:pts[i].x+(pts[i+1].x-pts[i].x)*r,y:pts[i].y+(pts[i+1].y-pts[i].y)*r,progress:Math.min(1,travel/total)}}left-=len}return{...pts.at(-1),progress:1}}
function unlocked(done=0){return new Set(futureTowers.slice(0,Math.min(futureTowers.length,3+Math.floor(done/2))).map(t=>t.id))}function stats(base,level=1){return{damage:Math.round(base.damage*(1+(level-1)*.4)),range:Math.round(base.range*(1+(level-1)*.07)),fireRate:Math.max(.28,base.fireRate*(1-(level-1)*.08))}}function gridPower(placed){let capacity=BASE_POWER,used=0;Object.values(placed).forEach(p=>{const t=futureTowers.find(x=>x.id===p.id);if(!t)return;if(t.power<0)capacity+=Math.abs(t.power);else used+=t.power});return{capacity,used,free:capacity-used}}
export function FutureBattle({mapNumber,save,setSave,onExit,onNextMap}){const world=save.worlds.future,map=futureMaps[mapNumber-1],layout=useMemo(()=>getFutureLayout(mapNumber),[mapNumber]),pathSet=useMemo(()=>new Set(layout.path),[layout]),pathTotal=useMemo(()=>pathLength(layout.path),[layout]),available=useMemo(()=>unlocked(world.completedMap),[world.completedMap]);
 const[credits,setCredits]=useState(START_CREDITS),[shield,setShield]=useState(MAX_SHIELD),[core,setCore]=useState(MAX_CORE),[wave,setWave]=useState(1),[selected,setSelected]=useState('pulse-turret'),[placed,setPlaced]=useState({}),[selectedCell,setSelectedCell]=useState(null),[enemies,setEnemies]=useState([]),[running,setRunning]=useState(false),[paused,setPaused]=useState(false),[speed,setSpeed]=useState(1),[message,setMessage]=useState('Construct the power grid before Wave 1.'),[status,setStatus]=useState('ready'),[kills,setKills]=useState(0);
 const creditsRef=useRef(credits),shieldRef=useRef(shield),coreRef=useRef(core),placedRef=useRef(placed),enemiesRef=useRef(enemies),runRef=useRef(running),pauseRef=useRef(paused),waveRef=useRef(wave),queue=useRef([]),cooldowns=useRef({}),finalized=useRef(false),killsRef=useRef(0),empRef=useRef(0);
 const syncCredits=v=>{creditsRef.current=v;setCredits(v)},syncShield=v=>{shieldRef.current=v;setShield(v)},syncCore=v=>{coreRef.current=v;setCore(v)},syncPlaced=v=>{placedRef.current=v;setPlaced(v)},syncEnemies=v=>{enemiesRef.current=v;setEnemies(v)},syncRun=v=>{runRef.current=v;setRunning(v)};useEffect(()=>{pauseRef.current=paused},[paused]);useEffect(()=>{waveRef.current=wave},[wave]);const power=gridPower(placed);const preview=useMemo(()=>summarizeFutureWave(buildFutureWave({mapNumber,waveNumber:wave})).slice(0,5),[mapNumber,wave]);
 const complete=()=>{if(finalized.current)return;finalized.current=true;syncRun(false);setStatus('won');const cores=1+(coreRef.current>=150?1:0)+(gridPower(placedRef.current).free>=0?1:0);setSave(prev=>{const old=prev.worlds.future,prior=old.best?.[map.id]?.dataCores??0,added=Math.max(0,cores-prior);const nextWorld={...old,completedMap:Math.max(old.completedMap??0,mapNumber),highestMap:Math.min(25,Math.max(old.highestMap??1,mapNumber<25?mapNumber+1:25)),dataCores:Math.min(75,(old.dataCores??0)+added),mastery:Math.min(100,(old.mastery??0)+2+Math.floor(killsRef.current/30)),best:{...(old.best??{}),[map.id]:{dataCores:Math.max(prior,cores),core:coreRef.current,shield:shieldRef.current,kills:killsRef.current}}};const next={...prev,worlds:{...prev.worlds,future:nextWorld}};if(mapNumber===25)next.worlds.space={...next.worlds.space,unlocked:true};return next});setMessage(`GRID STABLE • ${cores}/3 Data Cores secured.`)};const fail=()=>{if(finalized.current)return;finalized.current=true;syncRun(false);queue.current=[];setStatus('lost');setMessage('CORE OFFLINE — the defense network collapsed.')};
 const finishWave=()=>{syncRun(false);const relays=Object.values(placedRef.current).filter(p=>p.id==='shield-relay').length;syncShield(Math.min(MAX_SHIELD,shieldRef.current+25+relays*15));syncCredits(creditsRef.current+75+waveRef.current*12);if(waveRef.current>=FUTURE_WAVES){complete();return}const next=waveRef.current+1;waveRef.current=next;setWave(next);setMessage(`Wave clear. Shield recharged. Grid ready for Wave ${next}.`)};const start=()=>{if(runRef.current||finalized.current)return;const units=buildFutureWave({mapNumber,waveNumber:waveRef.current});queue.current=units.map((u,i)=>({...u,uid:`f-${Date.now()}-${i}`,delay:i*560,travel:0,x:futureCellCenter(layout.path[0]).x,y:futureCellCenter(layout.path[0]).y,dead:false,abilityClock:2.8}));syncRun(true);setStatus('running');setMessage(units.some(u=>u.boss)?`🚨 BOSS SIGNAL: ${units.find(u=>u.boss).name}`:`Wave ${waveRef.current} connected.`)};
 const place=cell=>{if(runRef.current||pathSet.has(cell))return;if(placedRef.current[cell]){setSelectedCell(cell);return}const tower=futureTowers.find(t=>t.id===selected);if(!tower||!available.has(tower.id)||creditsRef.current<tower.cost)return;const current=gridPower(placedRef.current);if(tower.power>0&&current.free<tower.power){setMessage('Insufficient grid Power. Build a Relay or Fusion Reactor.');return}syncCredits(creditsRef.current-tower.cost);syncPlaced({...placedRef.current,[cell]:{id:tower.id,level:1,invested:tower.cost}});setSelectedCell(cell)};const sell=()=>{const p=placedRef.current[selectedCell];if(!p)return;syncCredits(creditsRef.current+Math.round(p.invested*.65));const next={...placedRef.current};delete next[selectedCell];syncPlaced(next);setSelectedCell(null)};const upgrade=()=>{const p=placedRef.current[selectedCell],base=futureTowers.find(t=>t.id===p?.id);if(!p||!base||p.level>=3)return;const cost=Math.round(base.cost*(p.level===1?.7:1));if(creditsRef.current<cost)return;syncCredits(creditsRef.current-cost);syncPlaced({...placedRef.current,[selectedCell]:{...p,level:p.level+1,invested:p.invested+cost}})};
 useEffect(()=>{const timer=setInterval(()=>{if(!runRef.current||pauseRef.current)return;const dt=.05*speed;empRef.current=Math.max(0,empRef.current-dt);const spawn=[],wait=[];queue.current.forEach(e=>{const d=e.delay-dt*1000;(d<=0?spawn:wait).push(d<=0?{...e,delay:0}:{...e,delay:d})});queue.current=wait;let active=[...enemiesRef.current.map(e=>({...e})),...spawn];active.forEach(e=>{if(e.healer){e.abilityClock-=dt;if(e.abilityClock<=0){e.abilityClock=3;active.forEach(x=>{if(!x.dead&&distance(x,e)<150)x.hp=Math.min(x.maxHp,x.hp+x.maxHp*.07)})}}if(e.emp){e.abilityClock-=dt;if(e.abilityClock<=0){e.abilityClock=4.5;empRef.current=1.5;setMessage('⚡ EMP pulse disrupted the grid!')}}if(e.hacker){e.abilityClock-=dt;if(e.abilityClock<=0){e.abilityClock=5;const keys=Object.keys(placedRef.current);if(keys.length)cooldowns.current[keys[Math.floor(Math.random()*keys.length)]]=(cooldowns.current[keys[Math.floor(Math.random()*keys.length)]]??0)+2}}let mult=e.phase?.92:1;e.travel+=e.speed*mult*dt;const pos=point(layout.path,e.travel);e.x=pos.x;e.y=pos.y;e.progress=pos.progress;e.hidden=e.cloaked});
 const hasDetector=Object.entries(placedRef.current).some(([cell,p])=>{const t=futureTowers.find(x=>x.id===p.id);return t?.id==='detector-array'});for(const[cell,p]of Object.entries(placedRef.current)){const base=futureTowers.find(t=>t.id===p.id);if(!base||base.damage<=0)continue;const key=String(cell);cooldowns.current[key]=(cooldowns.current[key]??0)-dt;if(cooldowns.current[key]>0||empRef.current>0)continue;const s=stats(base,p.level),origin=futureCellCenter(Number(cell));let candidates=active.filter(e=>!e.dead&&(!e.hidden||hasDetector||base.id==='detector-array')&&distance(origin,e)<=s.range);if(!candidates.length)continue;candidates.sort((a,b)=>b.progress-a.progress);const target=candidates[0];let dmg=Math.max(1,Math.round(s.damage*(1-Math.min(.8,target.armor??0))));const hits=(base.id==='plasma-cannon'||base.id==='drone-bay')?candidates.slice(0,base.id==='drone-bay'?3:5):[target];hits.forEach(e=>{let value=dmg;if(e.shieldHp>0){const absorbed=Math.min(e.shieldHp,value);e.shieldHp-=absorbed;value-=absorbed}e.hp-=value;if(base.id==='emp-node')e.speed=Math.max(18,e.speed*.965);if(e.hp<=0)e.dead=true});cooldowns.current[key]=s.fireRate}
 let shieldNow=shieldRef.current,coreNow=coreRef.current,earned=0,killed=0;const survivors=[];active.forEach(e=>{if(e.dead||e.hp<=0){killed++;earned+=e.creditReward??8;return}if(e.travel>=pathTotal){let damage=e.coreDamage??12;const shieldHit=Math.min(shieldNow,damage);shieldNow-=shieldHit;damage-=shieldHit;if(damage>0)coreNow=Math.max(0,coreNow-damage);return}survivors.push(e)});if(killed){killsRef.current+=killed;setKills(killsRef.current);syncCredits(creditsRef.current+earned)}syncShield(shieldNow);syncCore(coreNow);syncEnemies(survivors);if(coreNow<=0){fail();return}if(queue.current.length===0&&survivors.length===0)finishWave()},50);return()=>clearInterval(timer)},[layout,pathTotal,speed,mapNumber]);
 const selectedData=selectedCell!=null?placed[selectedCell]:null,selectedBase=selectedData?futureTowers.find(t=>t.id===selectedData.id):null;
 return <section className="future-battle"><header className="future-battle-hud"><div><b>🤖 {map.name}</b><small>{map.sector}</small></div><div><span>₡ {credits}</span><span>⚡ {power.used}/{power.capacity}</span><span>🛡️ {shield}/{MAX_SHIELD}</span><span>💙 {core}/{MAX_CORE}</span><span>🌊 {wave}/{FUTURE_WAVES}</span></div><div><button onClick={onExit} disabled={running}>⌂</button><button onClick={()=>setPaused(v=>!v)} disabled={!running}>{paused?'▶':'⏸'}</button><button onClick={()=>setSpeed(v=>v===1?2:v===2?3:1)}>{speed}×</button><button className="future-start" onClick={start} disabled={running||finalized.current}>DEPLOY {wave}</button></div></header><div className="future-body"><div className="future-board-wrap"><div className="future-preview"><b>{message}</b><span>{preview.map(x=>`${x.name} ×${x.count}`).join(' • ')}</span></div><div className="future-board">{CELLS.map(cell=>{const p=placed[cell],base=p?futureTowers.find(t=>t.id===p.id):null;return <button key={cell} className={`future-cell ${pathSet.has(cell)?'path':''} ${layout.powerNodes.includes(cell)?'power-node':''} ${p?'occupied':''} ${selectedCell===cell?'selected':''}`} onClick={()=>place(cell)}>{base?<span>{base.icon}<small>{p.level>1?`L${p.level}`:''}</small></span>:pathSet.has(cell)?'·':layout.powerNodes.includes(cell)?'⚡':''}</button>})}{enemies.map(e=><div key={e.uid} className={`future-enemy ${e.boss?'boss':''} ${e.hidden?'hidden':''}`} style={{left:`${e.x/12}%`,top:`${e.y/5}%`}}><span>{e.icon}</span><i><b style={{width:`${Math.max(0,e.hp/e.maxHp*100)}%`}}/></i></div>)}</div></div><aside className="future-build"><h3>DEFENSE GRID</h3><div className="future-towers">{futureTowers.map(t=>{const locked=!available.has(t.id),noPower=t.power>0&&power.free<t.power;return <button key={t.id} disabled={locked} className={`${selected===t.id?'selected':''} ${noPower?'no-power':''}`} onClick={()=>setSelected(t.id)}><span>{t.icon}</span><div><b>{t.name}</b><small>{locked?'LOCKED':t.role}</small></div><em>₡{t.cost} • {t.power<0?`+${Math.abs(t.power)}`:`-${t.power}`}⚡</em></button>})}</div>{selectedBase?<div className="future-selected-tower"><b>{selectedBase.icon} {selectedBase.name}</b><span>Level {selectedData.level}</span><div><button onClick={upgrade} disabled={selectedData.level>=3}>Upgrade</button><button onClick={sell}>Sell</button></div></div>:<div className="future-grid-tip"><b>GRID STATUS</b><span>{power.free>=0?`${power.free} spare Power`:'POWER OVERLOAD'}</span><small>Reactors and Power Relays increase capacity.</small></div>}</aside></div>{(status==='won'||status==='lost')&&<div className="future-result"><div><strong>{status==='won'?'SYSTEM SECURE':'CORE OFFLINE'}</strong><h2>{message}</h2><button onClick={onExit}>Campaign</button>{status==='won'&&mapNumber<25&&<button onClick={()=>onNextMap(mapNumber+1)}>Next Sector →</button>}</div></div>}</section>}
