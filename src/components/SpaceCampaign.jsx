import React from'react';import{spaceMaps}from'../data/worlds/space/maps.js';
const regions=['Home System','Alien Frontier','Deep Space','War Zone','End of Space'];
export function SpaceCampaign({save,selectedMap,setSelectedMap,onStart,onBack}){const world=save.worlds.space,map=spaceMaps[selectedMap-1],done=world.completedMap??0;return <section className="space-campaign"><header className="space-header"><div><small>CHRONO DEFENSE • ERA IV</small><h1>🚀 SPACE TOWER DEFENSE</h1><p>Defend the colony beyond the edge of the timeline.</p></div><div><span>⭐ {world.starCores??0}/75</span><span>🌌 Colony Fleet</span><span>🔥 Mastery {world.mastery??0}</span><button onClick={onBack}>← Future</button></div></header><div className="space-regions">{regions.map(r=><span className={map.region===r?'active':''} key={r}>{r}</span>)}</div><main className="space-campaign-body"><div className="space-map-grid">{spaceMaps.map(item=>{const locked=item.number>(world.highestMap??1),best=world.best?.[item.id];return <button key={item.id} disabled={locked} className={`space-map ${selectedMap===item.number?'selected':''} ${item.boss?'boss':''}`} onClick={()=>setSelectedMap(item.number)}><strong>{locked?'🔒':item.number}</strong><b>{item.icon} {item.name}</b><small>{item.mechanic}</small><em>{item.boss?'👑 ':''}{best?.starCores?'⭐'.repeat(best.starCores):'○○○'}</em></button>})}</div><aside className="space-side"><div className="space-progress"><small>EXPEDITION PROGRESS</small><b>{done}/25 systems secured</b><i><span style={{width:`${Math.round(done/25*100)}%`}}/></i></div><div className="space-selected"><div>{map.icon}{map.boss?'👑':''}</div><small>MAP {map.number} • {map.region}</small><h2>{map.name}</h2><p>{map.mechanic}</p>{map.starCores.map((goal,i)=><span key={goal}>⭐ {i+1}. {goal}</span>)}</div><div className="space-rules"><b>SPACE RULES</b><span>◆ Matter constructs orbital defenses.</span><span>⚡ Energy charges special technologies.</span><span>🛡️ Planetary Shield regenerates between waves.</span><span>🏙️ Colony Integrity is permanent during a map.</span><span>🌀 Gravity and wormhole towers manipulate paths.</span></div><button className="space-launch" onClick={onStart}>LAUNCH DEFENSE →</button></aside></main></section>}
