import React,{useState}from'react';
const steps=[['◆','Matter','Matter builds orbital defenses. Destroy enemy fleets to recover more.'],['⚡','Energy','Kills charge Energy. Spend 50 Energy on Orbital Pulse to damage and slow the entire active fleet.'],['🛡️','Planetary Shield','The shield regenerates between waves. Colony Integrity does not, so stop leaks before the shield collapses.'],['🌀','Gravity Control','Gravity Wells and Tractor Beams manipulate enemy movement, but some massive ships resist control.'],['🛰️','Detection','Stealth ships need Satellite Array coverage before most towers can target them.'],['⭐','Star Cores','Secure up to three Star Cores on each system. Finish Space to reveal the hidden Time Rift.']];
export function SpaceTutorial({onComplete}){const[index,setIndex]=useState(0),[icon,title,copy]=steps[index];return <div className="space-tutorial"><div><small>ERA IV FLIGHT BRIEFING • {index+1}/{steps.length}</small><span>{icon}</span><h2>{title}</h2><p>{copy}</p><div className="tutorial-dots">{steps.map((_,i)=><i key={i} className={i===index?'active':''}/>)}</div><footer><button onClick={onComplete}>Skip</button><button onClick={()=>index===steps.length-1?onComplete():setIndex(i=>i+1)}>{index===steps.length-1?'LAUNCH':'Next →'}</button></footer></div></div>}
