import React,{useEffect,useState}from'react';import{loadSave,persistSave}from'../core/save.js';import{TimeRiftCampaign}from'./TimeRiftCampaign.jsx';import{TimeRiftBattle}from'./TimeRiftBattle.jsx';
export function TimeRiftExperience({onSwitchWorld}){const[save,setSave]=useState(()=>loadSave()),world=save.worlds['time-rift'],[screen,setScreen]=useState('campaign'),[selectedMap,setSelectedMap]=useState(()=>Math.min(12,Math.max(1,world.highestMap??1))),[battleKey,setBattleKey]=useState(0);useEffect(()=>persistSave(save),[save]);useEffect(()=>{if(!world.unlocked)onSwitchWorld?.('space')},[world.unlocked,onSwitchWorld]);const start=()=>{setBattleKey(v=>v+1);setScreen('battle')},back=()=>onSwitchWorld?.('space'),exit=()=>{setSelectedMap(Math.min(12,Math.max(1,save.worlds['time-rift'].highestMap??1)));setScreen('campaign')},next=map=>{setSelectedMap(map);setBattleKey(v=>v+1);setScreen('battle')};return screen==='campaign'?<TimeRiftCampaign save={save} selectedMap={selectedMap} setSelectedMap={setSelectedMap} onStart={start} onBack={back}/>:<TimeRiftBattle key={`${selectedMap}-${battleKey}`} mapNumber={selectedMap} save={save} setSave={setSave} onExit={exit} onNextMap={next}/>}
