import React,{useState}from'react';
const steps=[['🌀','The Fracture','Time Rift unlocks only after all four eras are completed. The timeline now mixes their threats.'],['◇','Rift Currency','Defeated enemies release Rift currency. Every crossover defense uses the same endgame resource.'],['🧭','Timeline Stability','Enemies reaching the end damage Timeline Stability. At zero, the fracture consumes the map.'],['⏳','Rift Cells','Glowing Rift cells distort movement and accelerate enemies that cross nearby space.'],['🌐','Cross-Era Strategy','Use Stone Age force, Retro speed, Future precision, and Space control together.'],['👑','The Chronophage','Seal all twelve fractures to face the true final enemy of Chrono Defense.']];
export function TimeRiftTutorial({onComplete}){const[index,setIndex]=useState(0),[icon,title,copy]=steps[index];return <div className="rift-tutorial"><div><small>SECRET ENDGAME • {index+1}/{steps.length}</small><span>{icon}</span><h2>{title}</h2><p>{copy}</p><div className="tutorial-dots">{steps.map((_,i)=><i key={i} className={i===index?'active':''}/>)}</div><footer><button onClick={onComplete}>Skip</button><button onClick={()=>index===steps.length-1?onComplete():setIndex(i=>i+1)}>{index===steps.length-1?'ENTER RIFT':'Next →'}</button></footer></div></div>}
