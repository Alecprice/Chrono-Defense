const key=(worldId,mapNumber)=>`chrono-adaptive-assist:${worldId}:${mapNumber}`;
function store(){try{return sessionStorage}catch{return null}}
export function armAdaptiveAssist(worldId,mapNumber){try{store()?.setItem(key(worldId,mapNumber),'1')}catch{}}
export function clearAdaptiveAssist(worldId,mapNumber){try{store()?.removeItem(key(worldId,mapNumber))}catch{}}
export function adaptiveAssistActive(worldId,mapNumber){try{return store()?.getItem(key(worldId,mapNumber))==='1'}catch{return false}}
export function applyAdaptiveAssist(worldId,mapNumber,units=[]){if(!adaptiveAssistActive(worldId,mapNumber))return units;return units.map(unit=>{const hpScale=unit.boss?.88:.8,damageScale=.82,speedScale=.9;const maxHp=Math.max(1,Math.round((unit.maxHp??unit.hp??1)*hpScale)),hp=Math.min(maxHp,Math.max(1,Math.round((unit.hp??unit.maxHp??1)*hpScale)));return{...unit,hp,maxHp,speed:Math.max(8,(unit.speed??1)*speedScale,1),shield:unit.shield?Math.max(1,Math.round(unit.shield*.82)):unit.shield,shieldHp:unit.shieldHp?Math.max(1,Math.round(unit.shieldHp*.82)):unit.shieldHp,baseDamage:unit.baseDamage?Math.max(1,Math.round(unit.baseDamage*damageScale)):unit.baseDamage,coreDamage:unit.coreDamage?Math.max(1,Math.round(unit.coreDamage*damageScale)):unit.coreDamage,colonyDamage:unit.colonyDamage?Math.max(1,Math.round(unit.colonyDamage*damageScale)):unit.colonyDamage,stabilityDamage:unit.stabilityDamage?Math.max(1,Math.round(unit.stabilityDamage*damageScale)):unit.stabilityDamage,villageDamage:unit.villageDamage?Math.max(1,Math.round(unit.villageDamage*damageScale)):unit.villageDamage}})}
