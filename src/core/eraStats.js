import{activeDailyMission,completeDailyMission}from'./eraDaily.js';
function changedBest(before={},after={}){for(const key of Object.keys(after)){if(after[key]!==before[key])return{key,record:after[key]}}return null}
export function trackedEraBattleSetter(setSave,{worldId,challenge='normal',mapNumber=1}={}){return updater=>setSave(previous=>{const next=typeof updater==='function'?updater(previous):updater;if(!next?.worlds?.[worldId])return next;const before=previous.worlds?.[worldId]??{},after=next.worlds[worldId],changed=changedBest(before.best??{},after.best??{});if(!changed)return next;const record=changed.record??{},kills=Number(record.kills??0),daily=activeDailyMission(),dailyWin=Boolean(daily&&daily.world===worldId&&daily.mapNumber===mapNumber);const oldStats=after.stats??{},challengeWins={...(oldStats.challengeWins??{})};if(challenge&&challenge!=='normal')challengeWins[challenge]=(challengeWins[challenge]??0)+1;const stats={...oldStats,kills:(oldStats.kills??0)+kills,wavesCleared:(oldStats.wavesCleared??0)+10,mapsCompleted:(oldStats.mapsCompleted??0)+1,victories:(oldStats.victories??0)+1,bossesDefeated:(oldStats.bossesDefeated??0)+(mapNumber%5===0?1:0),challengeWins,dailyWins:(oldStats.dailyWins??0)+(dailyWin?1:0),lastVictoryAt:new Date().toISOString()};if(dailyWin)queueMicrotask(()=>completeDailyMission({won:true,kills,mapNumber,challenge}));return{...next,worlds:{...next.worlds,[worldId]:{...after,stats}}}})}
